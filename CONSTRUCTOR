#include <iostream>
#include <cstring>
using namespace std;

class Student
{
private:
    char *name;
    int marks;

public:
    Student(const char *n, int m)
    {
        name = new char[strlen(n) + 1];
        strcpy(name, n);
        marks = m;
    }

    Student(const Student &s)
    {
        name = new char[strlen(s.name) + 1];
        strcpy(name, s.name);
        marks = s.marks;
    }

    void setData(const char *n, int m)
    {
        delete[] name;
        name = new char[strlen(n) + 1];
        strcpy(name, n);
        marks = m;
    }

    void display()
    {
        cout << "Name: " << name << endl;
        cout << "Marks: " << marks << endl;
    }

    ~Student()
    {
        delete[] name;
    }
};

int main()
{
    Student s1("Rahul", 85);

    Student s2 = s1;

    s2.setData("Amit", 95);

    cout << "Student 1:" << endl;
    s1.display();

    cout << endl;

    cout << "Student 2:" << endl;
    s2.display();

    return 0;
}
